#!/usr/bin/make -f

# Force debhelper to use the plain Makefile
%:
	dh $@ --buildsystem=makefile

# Ensure it installs the binary to /usr/bin/
override_dh_auto_install:
	install -D -m 0755 overlay $(DESTDIR)/usr/bin/overlay
